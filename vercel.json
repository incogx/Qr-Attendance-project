{
  "buildCommand": "cd Attendence-Admin-Portal && npm install && npm run build",
  "outputDirectory": "Attendence-Admin-Portal/dist",
  "installCommand": "cd Attendence-Admin-Portal && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
